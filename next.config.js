import withVideos from 'next-videos';

const nextConfig = {
  // Add your additional Next.js configuration here
  // For example:
  // Some configurations...
};

export default withVideos(nextConfig);
